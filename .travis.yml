language: node_js

node_js:
- 6

sudo: false

matrix:
  fast_finish: true
  include:
  - os: osx
    osx_image: xcode8.2
  - os: linux
    dist: trusty

cache:
  yarn: true
  directories:
  - node_modules
  - "$HOME/.electron"
  - "$HOME/.cache"

script:
- yarn test
